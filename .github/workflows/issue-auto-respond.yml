name: Issue Auto Response

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  auto-respond:
    runs-on: ubuntu-latest
    if: github.event.issue.user.type != 'Bot'
    steps:
      - name: Auto-label by issue content
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issue = context.payload.issue;
            const body = issue.body || '';
            const title = issue.title || '';
            const labels = [];

            if (body.includes('버그 리포트') || /^\[BUG\]/i.test(title)) {
              labels.push('bug', 'triage');
            } else if (body.includes('기능 요청') || /^\[FEAT\]/i.test(title)) {
              labels.push('enhancement', 'triage');
            } else if (body.includes('문서') || /^\[DOCS\]/i.test(title)) {
              labels.push('documentation');
            }

            if (/긴급|critical|blocker/i.test(title + body)) {
              labels.push('priority:high');
            }

            if (labels.length > 0) {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                labels: labels,
              });
            }

      - name: Post welcome comment
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issue = context.payload.issue;
            if (issue.user.type === 'Bot') return;

            const body = [
              `@${issue.user.login} 이슈 등록 감사합니다.`,
              '',
              '담당자가 확인 후 응답드리겠습니다.',
              '',
              '**참고 문서:**',
              '- [Getting Started (Wiki)](https://github.com/umyunsang/GovOn/wiki/Getting-Started)',
              '- [Troubleshooting (Wiki)](https://github.com/umyunsang/GovOn/wiki/Troubleshooting)',
            ].join('\n');

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              body: body,
            });
